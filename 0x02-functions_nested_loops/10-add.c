#include <stdio.h>

/**
 * main - check the code
 *
 * Write a function that adds two integers and returns the result.
 * int add(int, int);
 *
 * Return: Always 0.
 */

int add(int x, int y)
{
    return x + y;
}
